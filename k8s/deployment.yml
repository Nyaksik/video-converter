apiVersion: apps/v1
kind: Deployment
metadata:
  name: astro-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: astro-app
  template:
    metadata:
      labels:
        app: astro-app
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: astro
          image: ghcr.io/nyaksik/video-converter:latest
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: astro-service
spec:
  selector:
    app: astro-app
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
