---
import { PUBLIC_INFO_EMAIL } from 'astro:env/server'
---

{PUBLIC_INFO_EMAIL && (
  <footer>
    <hr class="opacity-40" >
    <div class="container text-center py-5">
      <a class="text-xs" href={`mailto:${PUBLIC_INFO_EMAIL}`}>{PUBLIC_INFO_EMAIL}</a>
    </div>
  </footer>
)}
